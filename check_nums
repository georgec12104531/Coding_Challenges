# Input: Integer, Integer
# Output: string "true" or "false" or "-1"

# - return "-1" if num1 == num2


def CheckNums(num1,num2)
    return "-1" if num1 == num2
    num2 > num1 ? "true" : "false"
end
   
