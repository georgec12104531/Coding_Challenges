def LetterCapitalize(str)
    string_arr = str.split(" ")
    #["hello", "world"]
    string_arr.map do |word|
        word_split = word[0].upcase + word[1..-1]
        word = word_split
    end
    .join(" ")
end
